configfile: "config.yaml"

repetitions = list(range(5))
error_rates = config["er_rates"]
min_len = config["min_len"]

rule make_all:
	input:
		"table1.tsv"
	shell:
		"echo 'Done'"

include: "rules/simulate.smk"
include: "rules/stellar.smk"
include: "rules/evaluate.smk"

